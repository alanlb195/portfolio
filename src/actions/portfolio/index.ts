import { getCertificatesPaginated } from "./get-certificates-paginated";
import { getProjectsPaginated } from "./get-projects-paginated";

export const server = {
    getCertificatesPaginated,
    getProjectsPaginated,
}