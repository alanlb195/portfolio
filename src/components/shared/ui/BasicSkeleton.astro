---

interface Props {
    width?: string;
    height?: string;
}

const {
    width = "w-full",
    height = "h-4",
} = Astro.props;
---

<div
    aria-hidden="true"
    class={`bg-gray-300/70 dark:bg-gray-700/60 animate-pulse rounded-lg ${width} ${height}`}
>
</div>
